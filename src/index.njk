---
layout: layouts/base.njk
---

<header class="cfc-header">

  <div class="container">

    <img src="_assets/logo.svg" alt="" />
    <h1>{{ basics.name }}</h1>

    {% if events[0].date | isFutureDate %}
      {% if events[0].url %}
        <a href="{{ events[0].url }}">Join us at the next meetup</a>
      {% endif %}
  
    {% endif %}

  </div>

</header>

<div class="container cfc-events">

  <section>

    <h2>
    {% if events[0].date | isFutureDate %}
      Upcoming event
    {% else %}
      Latest event
    {% endif %}
    </h2>

    <h3>{{ events[0].name }}</h3>
    <date>{{ events[0].date | readableDate }}</date>
    <p>{{ events[0].description }}</p>
    {% if events[0].url %}
      <a href="{{ events[0].url }}" target="_blank">
      {% if events[0].date | isFutureDate %}
        More information and sign up
      {% else %}
        See more
      {% endif %}
    {% endif %}
    </a>
  </section>

  <section>

    <h2>Previous events</h2>

    <ul>
    {% set len = events.length %} 
    {% for i in range(1, len) %}
      <li>
        <h3>{{ events[i].name }}</h3>
        <date>{{ events[i].date | readableDate }}</date>
        {% if events[i].url %}
          <a href="{{ events[i].url }}" target="_blank">See more</a>
        {% endif %}
      </li>
    {% endfor %}
      
    </ul>

  </section>

</div>
